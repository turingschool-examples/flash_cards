# open = File.open(ARGV[0],  "r")
# cards_txt = open.read
# open.close


class CardGenerator
    def initialize(file)
        # open like we did above then
        # want to break each line and iterate over each line. 
        # make an instance of each line passed into each line as a card.
    end
end

# def break_by_line
#     line_num=0
#     open = File.open(ARGV[0],  "r")
#     cards_txt = open.read
#     cards_txt.gsub!(/\r\n?/, "\n")
#     open.each do |line|
#     puts "#{line_num += 1} #{line}"
#     end
# end
# break_by_line

