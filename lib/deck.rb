class Deck
  attr_reader :cards
  #
  def initialize(cards)
    @cards = cards
  end

def count
  @cards.count
#
end

def cards_category(category)

  @cards.each do |card|
    if card.category == cards_category
      stem_cards << card

    end
#
     stem_cards

   end
end
