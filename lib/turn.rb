class Turn

  def initialize(string, card)
    @string = string
    @card = card
    @guess = guess
  end

  def guess(guess)
    
  #   guess = gets.chomp
  #     if @guess == answer or @guess.downcase == answer
  #       puts "Correct. The answer is #{@guess}."
  #     else
  #       puts "Incorrect. The answer is #{@guess}."
  #     end
  end
