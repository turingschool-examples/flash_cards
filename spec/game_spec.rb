require './lib/card'

RSpec.describe Card do
  before(:each) do
    # code to run before each testx
  end
  it 'exists' do
    # create a Game and expect it .to
    # be_instance_of(Card)
  end

  xit 'has something' do
  end
end
